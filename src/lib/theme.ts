/**
 * Hex values for consumers that cannot resolve CSS variables
 * (Scalar theme injection, next/og / icon generation).
 */
export const themeColors = {
  background: "#09090b",
  surface: "#111113",
  surface2: "#18181b",
  foreground: "#fafafa",
  muted: "#a1a1aa",
  border: "rgba(255, 255, 255, 0.08)",
  accent: "#ef4444",
} as const;
